{
  "name": "speed-to-lead",
  "private": false,
  "description": "n8n speed-to-lead workflow: source tagging and email dedup.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node scripts/build-workflow.js",
    "import": "bash scripts/import.sh",
    "demo": "bash scripts/test-leads.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
