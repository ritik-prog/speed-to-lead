{
  "name": "speed-to-lead",
  "private": true,
  "description": "EmaReach / ritiktechs n8n add-on: speed-to-lead with source tagging and email dedup.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node scripts/build-workflow.js",
    "import": "bash scripts/import.sh",
    "demo": "bash scripts/test-leads.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
